export enum Locales {
  ENGLISH = "en",
  FRENCH = "fr",
}

export enum CookiesKey {
  THEME = "theme",
  LOCALE = "locale"
}

export enum MenuLinks {
  HOME = "home",
  ABOUT = "about",
  SKILLS = "skills",
  PROJECTS = "projects",
  CONTACT = "contact",
}
